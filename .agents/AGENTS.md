# Workspace Rules for Finly Finance

- **No Automatic Git Push**: Do NOT run `git push` to GitHub or trigger Vercel deployment automatically after editing code. Keep all changes saved locally and wait for explicit user confirmation before pushing to remote/GitHub/Vercel.
